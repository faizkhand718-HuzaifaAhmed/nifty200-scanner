sqlalchemy>=2.0,<3.0
pydantic>=2.0,<3.0
psycopg2-binary>=2.9,<3.0
pytest>=7.4,<9.0
tzdata>=2024.1  # ensures IANA tz database is available on all platforms (zoneinfo)
pandas>=2.0,<4.0
numpy>=1.24,<3.0
scikit-learn>=1.3,<2.0

# --- Production server (added for deployment) ---
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
websockets>=12.0,<13.0

# --- Error monitoring (OPTIONAL - only used if SENTRY_DSN is set;
# app.core.error_monitoring degrades gracefully if this isn't installed) ---
sentry-sdk>=1.40,<3.0

# --- NSE MCP integration (OPTIONAL - only imported when
# MARKET_DATA_PROVIDER=nse_mcp; app/market_data/mcp_client.py's real
# network calls could not be exercised in this project's dev sandbox, no
# network access there - see the project's status report) ---
mcp>=1.0,<2.0
